import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.keogblem.taskin',
  appName: 'TaskIn',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
